# Dependencias
node_modules/

# Variables de entorno (mantener .env.example en el repo si lo usas)
.env
.env.local
.env.*.local

# Claves y credenciales (Firebase / Google Cloud / similares)
serviceAccountKey*.json
*-firebase-adminsdk-*.json
firebase-adminsdk*.json
*.pem

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Tests y cobertura (Vitest)
coverage/

# Caché y temporales
.cache/
*.tmp
*.temp

# Salidas de build (por si añades pasos más adelante)
dist/
build/

# Sistema operativo
.DS_Store
Thumbs.db
